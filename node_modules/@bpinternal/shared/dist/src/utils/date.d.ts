export type DateFormatOptions = {
    format?: 'full' | 'long' | 'medium' | 'short';
    relative?: boolean;
    includeTime?: boolean;
};
export declare function formatDate(date: string | Date, options?: DateFormatOptions): string | null;
export declare const isoDateRegex: RegExp;
