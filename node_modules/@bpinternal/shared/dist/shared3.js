import t from "./shared5.js";
function d(o, i = {}) {
  const { format: a = "medium", relative: n = !1, includeTime: r = !1 } = i, e = typeof o == "string" ? t.fromISO(o) : t.fromJSDate(o);
  if (!e.isValid)
    return "Invalid date";
  if (n)
    return e.toRelative();
  switch (a) {
    case "full":
      return e.toLocaleString({ ...t.DATE_HUGE, ...r && t.TIME_SIMPLE });
    case "long":
      return e.toLocaleString({ ...t.DATE_FULL, ...r && t.TIME_SIMPLE });
    case "short":
      return e.toLocaleString({ ...t.DATE_SHORT, ...r && t.TIME_SIMPLE });
    default:
      return e.toLocaleString({
        ...t.DATE_MED,
        ...r && t.TIME_SIMPLE
      });
  }
}
const s = /^\d{4}-\d{2}-\d{2}(?:[T\s]\d{2}:\d{2}(?::\d{2}(?:\.\d{1,3})?)?(?:Z|[+\-]\d{2}:\d{2})?)?$/;
export {
  d as formatDate,
  s as isoDateRegex
};
